# Stash Override

This repository hosts a Stash override file for subscription merging.

## Files

- `stash-override.yaml`: override rules for Douyin direct routing and Apple AI proxy routing.

## Usage

Use the raw URL of `stash-override.yaml` in your Stash override configuration.
